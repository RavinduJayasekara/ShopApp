class Item {
  constructor(quantity, prodTitle, prodPrice, sum) {
    (this.quantity = quantity),
      (this.prodTitle = prodTitle),
      (this.prodPrice = prodPrice),
      (this.sum = sum);
  }
}

export default Item;
